{
  "name": "bugsite-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal Express + MongoDB API for BugSite. Serves read-only product reference data so the catalog can be browsed in MongoDB Compass. Does NOT touch any of the 28 intentional frontend bugs.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "node seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongodb": "^6.12.0"
  }
}
